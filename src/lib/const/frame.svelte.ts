export const frame = $state({
    nameFrame: 'COOLEGANT',
    totalName: 2,
    totalPhoto: 3,

})

export type frame = {
    nameFrame: string;
    totalName: number;
    totalPhoto: number;
    totalFrame: number
}

